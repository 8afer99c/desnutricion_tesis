services:
  - type: web
    name: api-desnutricion
    runtime: python
    rootDir: API_DESNUTRICION/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT